version: '3'
services:
  mariadb:
    image: mariadb:10.4.27
    container_name: my-mariadb-container
    environment:
      MYSQL_ROOT_PASSWORD: p@ss
      MYSQL_DATABASE: streaming
      MYSQL_USER: streaming_user
      MYSQL_PASSWORD: kdsjgiaulrngi32p859h3pnt8183t1
    ports:
      - "3306:3306"
    volumes:
      - ./streaming.sql:/docker-entrypoint-initdb.d/init.sql

  flaskapp:
    build: ./python
    environment:
      SERVER_IP: 172.16.1.14
      MYSQL_HOST: 172.16.1.14
      MYSQL_USER: streaming_user
      MYSQL_DATABASE: streaming
      MYSQL_PASSWORD: kdsjgiaulrngi32p859h3pnt8183t1
    ports:
      - "8900:8900"
    volumes:
      - ./python:/app

  react-app:
    build:
      context: .
      dockerfile: ./Dockerfile.prod
    ports:
      - "80:80"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf
